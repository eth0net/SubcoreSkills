using Verse;

namespace SubcoreSkills
{
    internal class SubcoreSkillsSettings : ModSettings
    {
        /// <summary>
        /// ExposeData saves and loads the settings.
        /// </summary>
        public override void ExposeData()
        {
            base.ExposeData();
        }
    }
}
